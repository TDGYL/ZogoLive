import 'package:flutter/material.dart';

/// 颜色常量定义
class G5Colors {
  /// 深黑微光主背景
  static const Color pitch = Color(0xFF0B0E14);

  /// 卡片模块背景
  static const Color pitchCard = Color(0xFF141923);

  /// 悬浮层背景
  static const Color pitchElevated = Color(0xFF1D2432);

  /// 边框颜色
  static const Color pitchBorder = Color(0xFF283142);

  /// 强调色 - 翡翠绿 (主色调)
  static const Color accentEmerald = Color(0xFF10B981);

  /// 强调色 - 直播绿
  static const Color accentLime = Color(0xFF84CC16);

  /// 强调色 - 黄金色
  static const Color accentGold = Color(0xFFF59E0B);

  /// 强调色 - 警告红
  static const Color accentCrimson = Color(0xFFEF4444);

  /// 强调色 - 战术蓝
  static const Color accentBlue = Color(0xFF3B82F6);

  /// 文本颜色 - 主要
  static const Color textPrimary = Color(0xFFF1F5F9); // slate-100
  /// 文本颜色 - 次要
  static const Color textSecondary = Color(0xFF94A3B8); // slate-400
}
